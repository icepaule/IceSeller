# eBay API
EBAY_APP_ID=your_app_id
EBAY_CERT_ID=your_cert_id
EBAY_DEV_ID=your_dev_id
EBAY_REDIRECT_URI=your_redirect_uri
EBAY_ENVIRONMENT=SANDBOX
EBAY_MARKETPLACE=EBAY_DE

# Ollama
OLLAMA_HOST=http://localhost:11434
OLLAMA_MODEL=
# Leave empty for auto-detection (prefers minicpm-v > qwen2.5vl > llava)
# Or set explicitly: minicpm-v:8b, qwen2.5vl:7b, llava:13b

# DHL Parcel DE Shipping API v2
DHL_API_KEY=your_dhl_api_key
DHL_API_SECRET=your_dhl_api_secret
DHL_USERNAME=user-valid
DHL_PASSWORD=SandboxPasswort2023!
DHL_ENVIRONMENT=SANDBOX
DHL_BILLING_NUMBER=your_ekp_number

# Sender address for DHL
SENDER_NAME=Max Mustermann
SENDER_STREET=Musterstrasse 1
SENDER_POSTAL_CODE=12345
SENDER_CITY=Berlin
SENDER_COUNTRY=DE

# Gmail SMTP
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your@gmail.com
SMTP_PASSWORD=your_app_password
NOTIFY_EMAIL=your@gmail.com

# Camera
CAMERA_DEVICE=/dev/video0
CAMERA_TYPE=usb

# App
APP_HOST=0.0.0.0
APP_PORT=8080
DATA_DIR=/app/data
SECRET_KEY=change-me-to-a-random-string
